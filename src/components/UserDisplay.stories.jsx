import UserDisplay from './UserDisplay';

const meta = {
  component: UserDisplay,
  tags: ['ai-generated'],
};

export default meta;

export const Default = {};

export const WithData = {
  args: {
    data: { name: 'John Doe', country: 'USA' },
  },
};
